{
  "name": "@paxpar-tech/pp-template-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@paxpar-tech/pp-widgets-demo": "../pp-widgets-demo"
  }
}
